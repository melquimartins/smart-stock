services:
  db:
    image: postgres:15
    container_name: smart-stock-pg
    restart: unless-stopped
    environment:
      POSTGRES_DB: smart_stock
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
